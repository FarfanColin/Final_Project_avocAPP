{{!--The signup page will verificate the data entered by the user and create a new account--}}
<div class="login-box">
    <h2>Sign Up</h2>
    {{!--By the variable "hasErrors" is gonna be possible to use our "Flash" package, then display a message indicating
    where the error is coming from--}}
    {{#if hasErrors}}
    <div class="alert alert-danger">
        {{# each messages }}
        <p>{{this}}</p>
        {{/each}}
    </div>
    {{/if}}
    <form action="/user/signup" method="post">
        <div class="user-box">
            <input type="text" name="fname" required="fname">
            <label>Name</label>
        </div>
        <div class="user-box">
            <input type="text" name="sname" required="sname">
            <label>Surname</label>
        </div>
        <div class="user-box">
            <input type="text" name="phone" required="phone">
            <label>Phone</label>
        </div>
        <div class="user-box">
            <input type="text" name="email" required="email">
            <label>E-Mail</label>
        </div>
        <div class="user-box">
            <input type="password" name="password" required="password">
            <label>Password</label>
        </div>
        {{!--This is the token tha csrf will use to protect the session once the user is navigating through the web
        application, the configuration will hide the token by itself--}}
        <input type="hidden" name="_csrf" value="{{ csrfToken }}">
        <p class="full">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
            <button type="submit">Sign Up</button>
        </p>
    </form>
</div>